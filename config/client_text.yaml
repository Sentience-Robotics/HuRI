huri_url: ws://localhost:8000/session

interface_path: src.interfaces.cli_interface:cli_interface

senders:
  text:
    name: text
    topic: question

hooks:
  text:
    name: text
    topics: [rag_response]

modules:
  rag:
    name: rag
    args:
      language: en
      tone: formal
    logging: INFO
