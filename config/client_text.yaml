huri_url: ws://localhost:8000/session

topic_list: [question, token]

senders:
  text:
    name: text

modules:
  rag:
    name: rag
    args:
      language: en
      tone: formal
      max_history_turns: 6
      temperature: 0.7
      response_format: short
      persona: ""
